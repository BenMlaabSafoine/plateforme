{% extends 'base.html.twig' %}

{% block title %}Offre index{% endblock %}

{% block body %}
    <h1>Offre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date_debut</th>
                <th>Date_fin</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for offre in offres %}
            <tr>
                <td>{{ offre.id }}</td>
                <td>{{ offre.dateDebut ? offre.dateDebut|date('Y-m-d') : '' }}</td>
                <td>{{ offre.dateFin ? offre.dateFin|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('offre_show', {'id': offre.id}) }}">show</a>
                    <a href="{{ path('offre_edit', {'id': offre.id}) }}">edit</a>
                </td>
            </tr>
                        <tr>
                            <th>GrilleTarifaire</th>
                                                        <th>Action</th>

                        </tr>
                                {% for o in offre.grilletarifaires %}
                        <tr>
                                       <td>{{ o.description }}</td>
                                        <td>
<form method="post" action="{{ path('grille_tarifaire_del', {'id': o.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ o.id) }}">
    <button class="btn">Delete</button>
</form>

     </td>


            </tr>
            {% endfor %}
             <tr>
                            <th>Pays</th>
                                                        <th>Action</th>

                        </tr>
                                {% for o in offre.pays %}
                        <tr>
                                       <td>{{ o.nom}}</td>
                                        <td>
<form method="post" action="{{ path('pays_del', {'id': o.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ o.id) }}">
    <button class="btn">Delete</button>
</form>

     </td>


            </tr>
        {% endfor %}

        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('offre_new') }}">Create new</a>
{% endblock %}
