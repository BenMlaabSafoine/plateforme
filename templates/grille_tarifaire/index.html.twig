{% extends 'base.html.twig' %}

{% block title %}GrilleTarifaire index{% endblock %}

{% block body %}
    <h1>GrilleTarifaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                                <th>offre</th>

                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for grille_tarifaire in grille_tarifaires %}
            <tr>
                <td>{{ grille_tarifaire.id }}</td>
                <td>{{ grille_tarifaire.description }}</td>
                <td>{{ grille_tarifaire.offre.id }}</td>


                <td>
                    <a href="{{ path('grille_tarifaire_show', {'id': grille_tarifaire.id}) }}">show</a>
                    <a href="{{ path('grille_tarifaire_edit', {'id': grille_tarifaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('grille_tarifaire_new') }}">Create new</a>
{% endblock %}
