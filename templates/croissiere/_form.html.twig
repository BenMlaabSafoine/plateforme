{{ form_start(form) }}
    {{ form_widget(form) }}
     Description de Grille tarifaire : <br>
    <input type ="text" name ="Description" id="Description">
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
